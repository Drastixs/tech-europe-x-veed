# Architecture

The supported product runtime is a Chromium MV3 extension and local FastAPI
services. See [dependencies.md](dependencies.md) for the supported components
and [contracts/runtime-v1.schema.json](contracts/runtime-v1.schema.json) for
the versioned cross-runtime payloads.
